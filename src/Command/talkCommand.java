package Command;

import Game.Player;

public class talkCommand implements Command {
    private Character character;

    public talkCommand(Character character) {
    }

    public void execute(Player player) {
    }
}